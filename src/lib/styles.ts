import * as RX from "reactxp";

const Styles = {
  absoluteFill: RX.Styles.createViewStyle({
    position: "absolute",
    top: 0,
    bottom: 0,
    left: 0,
    right: 0
  })
};

export default Styles;
